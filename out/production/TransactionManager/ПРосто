app/
│
├── entity/
│     Wallet
│     Transaction
│     Category
│     TransactionType (enum)
│
├── repository/
│     WalletRepository
│     WalletRepositoryImpl
│     TransactionRepository
│     TransactionRepositoryImpl
│     CategoryRepository
│     CategoryRepositoryImpl
│
├── service/
│     TransactionService
│     WalletService
│     CategoryService
│
└── ui/ (або app)
      ConsoleApp / Main



🎯 1. Визнач, що це за тип системи

Твій проєкт — це:

Маленький фінансовий трекер з доменною логікою.

Тобто це не CRUD-довідник.
Тут є бізнес-правила:

баланс не може бути від’ємним

транзакція змінює стан гаманця

є типи операцій (дохід / витрата)

є дата операції

Це означає → тобі потрібна чітка доменна модель, а не просто “класи з геттерами”.

🏗 2. Архітектура, яку варто зробити

Три рівні:

1️⃣ Domain (entity)

Тут живе бізнес-логіка.

Класи:

Wallet

Transaction

Category

TransactionType (enum)

Тут:

поля private

немає setAmount

немає роботи з Map

немає Scanner

немає System.out

Wallet має поведінку:

deposit

withdraw

apply

Transaction:

immutable (final поля)

дата створюється автоматично

2️⃣ Repository (збереження)

Тут:

Map

List

CRUD

Repository:

нічого не рахує

нічого не перевіряє

не знає, що таке withdraw

Він просто зберігає і повертає об'єкти.

3️⃣ Service (use-case рівень)

Service = бізнес-сценарії.

Наприклад:

createTransaction

createWallet

transferMoney

getTransactionsByDate

Service:

отримує дані

викликає методи доменних об'єктів

зберігає через repository

координує процес

Сервіс не змінює стан напряму через setter.

4️⃣ UI (Console)

Тут:

меню

Scanner

вивід у консоль

UI працює тільки через service.

Ніколи:

UI → repository

Тільки:

UI → service
🔒 3. Принципи, які зроблять проєкт “правильним”
✅ 1. Інкапсуляція

Погано:

public setAmount

змінювати баланс ззовні

Добре:

private поля

зміна стану тільки через методи

✅ 2. Single Responsibility

Repository — тільки зберігає

Service — тільки координує

Entity — тільки бізнес-логіка

✅ 3. Іммутабельність де можливо

Transaction:

поля final

дата створюється в конструкторі

не змінюється після створення

✅ 4. Використання enum замість String

Погано:

"INCOME"
"EXPENSE"

Добре:

TransactionType.INCOME
TransactionType.EXPENSE
✅ 5. Інваріанти в домені

Wallet сам гарантує:

не можна зняти більше ніж є

не можна поповнити на 0 або мінус

🧠 4. Як виглядатиме “доросла” логіка

Коли створюється транзакція:

Service отримує дані

Дістає wallet

Дістає category

Викликає wallet.apply(type, amount)

Створює Transaction

Зберігає wallet

Зберігає transaction

Тобто:
Service координує
Domain виконує логіку
Repository зберігає

🧩 5. Чого не варто робити

❌ 10 сервісів для кожної дрібниці
❌ Сеттери для всього
❌ Бізнес-логіку в repository
❌ Логіку в UI
❌ Працювати з Map з UI

🚀 6. Якщо хочеш зробити реально красиво

Можеш додати:

transfer між гаманцями

звіт за місяць

підрахунок total income / total expense

розділення на income / expense категорії

Але після того, як структура стане чистою.

🎓 7. Якщо коротко — що зробить твій проєкт “правильним”

✔ Чітке розділення шарів
✔ Інкапсуляція
✔ enum замість String
✔ immutable Transaction
✔ Wallet керує своїм балансом
✔ Service — тільки координатор

📌 Найголовніше

Твоя задача зараз — не “зробити багато класів”.

Твоя задача — зробити так, щоб:

стан змінювався контрольовано

правила були всередині домену

логіка не розмазувалась по всьому коду